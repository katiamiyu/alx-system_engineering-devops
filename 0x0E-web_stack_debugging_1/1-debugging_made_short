#!/usr/bin/env bash
# configures nginxp server to listening on port 80 of all servers active ips (ipv4)

sudo sed -i "s/8080/80/" /etc/nginx/sites-enabled/default
sudo service nginx restart
echo "" | sudo dd status=none of=/run/nginx.pid
